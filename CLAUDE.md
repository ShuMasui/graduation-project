# Project: 高密度EEG解析ソフトウェア (卒業研究)

## 1. 概要 (Overview)
本リポジトリは、**卒業研究**における**高密度EEG（High-Density Electroencephalography）データの解析**を目的としたソフトウェアリポジトリです。

---

## 2. ディレクトリ構成 (Directory Structure)
プロジェクト全体は、管理コストを抑えるため最小限の階層構造で運用します。

```text
.
├── CLAUDE.md           # 本ファイル（プロジェクト方針・開発ガイドライン）
├── docs/               # 設計書、仕様、研究ノートなどのドキュメント
│   ├── exp-reproduce/  # Phase 1 再現実験ドキュメント群
│   └── agy/            # アーキテクチャ決定記録 (ADR)
├── exp-reproduce/      # Phase 1 再現実験プログラム本体 (Python 3.10)
└── eeg-processing/     # Phase 2 卒業研究本番用プログラム本体
```

- **`docs/`**: 設計方針、データ仕様、解析パイプラインの定義、ADR（`docs/agy/`）、研究メモ等のドキュメントを格納します。各Phaseの結果やノウハウは `docs/<Phase名>/outputs.md` に記録します。
- **`exp-reproduce/`**: Phase 1（再現実験）のプログラムコードおよび検証スクリプトを格納します。
- **`eeg-processing/`**: 卒業研究本番用の高密度EEGデータ処理・解析プログラムコードを格納します。

---

## 3. 開発・設計方針 & ノウハウ管理 (Design & Documentation Principles)

1. **Python による最小限の実装**
   - 言語は **Python** を使用します。
   - 必要最低限の機能のみを持つシンプルなソフトウェア構成を徹底します。
2. **KISS原則（Keep It Simple, Stupid）**
   - 不要な抽象化、過剰なフレームワーク導入、複雑なモジュール分割を避けます。
   - 「誰が見ても処理フローが自明であること」を重視します。
3. **ディレクトリ・ファイル数の最小化**
   - ディレクトリやファイルは必要最低限に留め、ファイルの散乱を防ぎます。
   - 管理コストおよび実証・検証（再現性確認）にかかるコストを極限まで低減します。
4. **実証性・再現性の優先**
   - 実験データに対する解析手順が明確で、検証を迅速に行える構造を維持します。
5. **Phaseごとの結果・ノウハウの記録 (`outputs.md`)**
   - **各研究Phaseの結果や実験で得られた知見・ノウハウは、`docs/` 配下の「各Phase名ディレクトリ」内にある `outputs.md`（例: `docs/exp-reproduce/outputs.md`）に記録・集約します。**
   - 各Phaseの検証結果やノウハウを確認する際は、該当ディレクトリの `outputs.md` を参照してください。
6. **数学的説明性の重視とライブラリ機能の活用（ベクトル化演算の徹底）**
   - 数学的な説明性を重視したコードとし、自前の `for` ループを極力排除します。
   - 済ませられる処理は NumPy や Pandas 等の研究実績のある標準ライブラリのベクトル化・行列演算機能に頼ることで、数式との整合性・再現性・実行性能を高めます。

---

## 4. 開発ロードマップ / TODO (Development Roadmap & TODO)

本プロジェクトは以下の3段階のフェーズで進めます。

- [ ] **Phase 1: EEG処理プログラムの再現実験 (`exp-reproduce`)**
  - `docs/exp-reproduce/paper/fnimg-4-1479569.pdf` の論文で提案されている手法・パイプラインを再現する。
  - ベースラインとなる高密度EEGデータ処理の検証と動作確認を行う。
  - 結果・知見は `docs/exp-reproduce/outputs.md` にまとめる。
- [ ] **Phase 2: 今回の研究におけるプログラムの実装**
  - 卒業研究固有の課題・目的に合わせた解析プログラムを実装する。
  - 結果・知見は `docs/<Phase名>/outputs.md` にまとめる。
- [ ] **Phase 3: 実際のデータ解析**
  - 実際の実験データを対象に解析を実行し、評価および結果の出力を行う。
  - 結果・知見は `docs/<Phase名>/outputs.md` にまとめる。
